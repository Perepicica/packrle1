Вариант 5 - pack-rle
Упаковывает -z или распаковывает -u указанный в аргументе командной строки текстовый файл.
Выходной файл указывается как -out filename.txt,
по умолчанию имя формируется из входного файла с добавлением расширения.
Реальзовать сжатие RLE (run-length encoding). Продумать алгоритм сжатия и формат файла,
при котором сжатие "неудачных" файлов не приведет к большому увеличению размера файла.
Кроме самой программы следует написать автоматические тесты к ней.

ОБЪЯСНЕНИЕ МОЕГО АЛГОРИТМА

Кол-во повторяющихмя знаков записывается с "-" перед (ex. "АААА" -> "-4A"
Это для того чтобы не перепутать счетчик с цифрами, которые могут находиться в документах.
Именно поэтому кодирование начинается с 3 повторяющихся знаков, чтобы не было увеличения размера файла
